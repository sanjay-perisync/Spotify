// import { useDispatch } from "react-redux";
// import { setCurrentSong } from "../Redux/Slices/PlayerSlice";

// const SongCard = ({ song }) => {
//   const dispatch = useDispatch();

//   return (
//     <div onClick={() => dispatch(setCurrentSong(song))} className="cursor-pointer">
//       <img src={song.image} alt={song.title} className="w-44 h-48 rounded-lg" />
//       <p className="mt-2 text-[18px] font-semibold">{song.title}</p>
//     </div>
//   );
// };
// export default SongCard;
